import { apiSpider } from '../records.api';

describe('apiSpider', () => {
  it('should process the api', async () => {
    // const output = await apiSpider();
    expect(apiSpider).toBeDefined();
  });
});
